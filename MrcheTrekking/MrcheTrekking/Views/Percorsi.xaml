<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:custom="clr-namespace:MrcheTrekking.Utility"
             x:Class="MrcheTrekking.Views.Percorsi" Title="Percorsi">
    
    <ContentPage.Content>
        <ListView  x:Name="lstView" ItemsSource="{Binding Percorsi}" ItemSelected="OnItemSelected"  RowHeight="80">
			<ListView.ItemTemplate >
				<DataTemplate>
                    <custom:CustomViewCell SelectedItemBackgroundColor="#adf3bc">  
					    <ViewCell.View>
						    <StackLayout Orientation="Horizontal" HorizontalOptions="Fill">
                                <StackLayout Orientation="Horizontal">
                                    <Image Source="{Binding Immagine}" HorizontalOptions="Start"/>
                                </StackLayout>
							    <StackLayout Orientation="Vertical">
								    <Label Text = "{Binding Nome}" FontSize="18" />
								    <Label Text = "{Binding Descrizione}" />
							    </StackLayout>
						    </StackLayout>
					    </ViewCell.View>
                    </custom:CustomViewCell>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
    </ContentPage.Content>
</ContentPage>
